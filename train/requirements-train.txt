# torch/torchvision are intentionally not pinned here.
# Install CUDA wheels separately for GPU training, for example:
# python -m pip install --force-reinstall torch==2.5.1 torchvision==0.20.1 --index-url https://download.pytorch.org/whl/cu121
ultralytics>=8.4.0
onnx>=1.16.0
